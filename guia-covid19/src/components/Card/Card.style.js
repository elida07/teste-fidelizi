import { styled } from '@mui/material';

export const FooterList = styled('div')`
    display: grid;
    place-items: center;
    margin: 12px;

    img {
        max-height: 8vh;
    }

    p {
        color: #492b7c;
        font-size: 28px;
        font-weight: 300;
    }
`;
